# Step 0.1 — Project Foundation

## Status
Completed on 2026-05-07.

## Scope Delivered
- Vite + React + TypeScript project initialized.
- Core folders created under `src/`.
- Dependencies installed: `zustand`, `framer-motion`, `react-router-dom`.
- App routing shell added:
  - `/menu`
  - `/game`
  - `/codex`
  - `/settings`
- `GameContext` shell created with provider and hook.
- Base screens created for menu/game/codex/settings.
- Gold-on-black style foundation added with:
  - global design tokens
  - base UI styles
  - atmospheric void background

## Validation
- `npm run lint` passed.
- `npm run build` passed.
- `npm run dev` starts correctly.

## Notes
- English-first setup is active.
- Fonts are loaded through Google Fonts CDN for quick iteration.
